class App.Models.User extends App.Model
  urlRoot: '<%=Settings.frontend.api_url%>/api/v1/users'

  url: ->
    if @id?
      "#{@urlRoot}/#{@id}?token=#{@authToken()}"
    else
      "#{@urlRoot}?token=#{@authToken()}"
